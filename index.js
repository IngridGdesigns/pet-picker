import { catBreedData } from '/data.js'

const options = document.getElementById("options");
const catActivityForm = document.getElementById("catbreed-choice")

function renderForm() {
    
    console.log(catBreedData)
}

renderForm()
